<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace = "com.kosta136th.mappers.userMapper">

	<select resultType="com.kosta136th.user.LoginSessionDTO" id="getLoginProfileByEmail">
		select email, nickname from (select user_email as email, user_password as password, user_nickname as nickname from user)T where email = #{email} and password = #{password}  
	</select>
	
	<insert id = "insertLoginProfileByEmail">
		insert into user(user_email, user_password, user_nickname) values(#{email}, #{password}, #{nickname});
	</insert>

</mapper>